enum {
    TYPE,

    IDENTIFY,
    INT_LITERAL,
    DOUBLE_LITERAL,
    EOL,

    IF,
    ELSE,
    WHILE,
    FOR,
    PRINT,

    EQ,                      // ==
    NE,                      // !=
    LT,                      // <
    LE,                      // <=
    GT,                      // >
    GE,                      // >=
    LBRACKET,                // [
    RBRACKET,                // ]
    POINT,                   // .
    ASSIGN,

    ADD,
    SUB,
    MUL,
    DIV,
    NOT,

    BOOL,
    CHAR,
    INT,
    DOUBLE,
    STRING,
    ARRAY,
};